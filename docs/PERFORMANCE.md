# PERF
- UI: Targeted ThemeApplier (min root). Compose: remember/Stable. Shallow XML trees.
- Logic: Parse off-thread. Avoid freq object allocation in render. Cache for rates only.
- Resource: Lifecycle-aware. onDestroyView cleanup. Batch Pref updates.
- Protocol: Profile (AS Profiler) before optimize. Readability > Premature opt. Virtualize large graphs.
