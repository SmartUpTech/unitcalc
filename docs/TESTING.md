# TESTING
- Layout: Unit (src/test), UI (src/androidTest).
- Targets: Models (Conversion/Unit), Calculator (Evaluation), Catalog (Validation).
- Example: CalculatorCatalogTest.
- Rules: JUnit 4. Mock context-dep components. Run ./gradlew test. No side effects/network.
